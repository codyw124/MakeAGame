#pragma once
#include <queue>
#include <utility>
#include "Property.h"

class Animation : public Property
{
private:
    //std::queue<std::pair<sprite,size_t>> animation_;
    
public:
    Animation();
    ~Animation();
};